import {env} from 'cloudflare:workers';
import {allMenus} from '@/lib/menu-catalog';
import {reply,failure} from '@/lib/server';
import {nowgoUrl,resolveOfficialStatus} from '@/lib/integration-policy';
export async function GET(req:Request,{params}:{params:Promise<{id:string}>}){try{const {id}=await params,menu=(await allMenus()).find(m=>m.id===id),unknown=resolveOfficialStatus(null,'','');if(!menu)return reply(req,unknown,404);if(menu.isDemo)return reply(req,{...unknown,source:'가매장 · 실제 영업하지 않음'});const e=env as unknown as Record<string,string>,u=nowgoUrl(e.NOWGO_STATUS_API_URL);if(!u)return reply(req,unknown);u.searchParams.set('hot_place_id',menu.placeId);u.searchParams.set('hot_menu_id',menu.id);const r=await fetch(u,{headers:e.NOWGO_STATUS_API_TOKEN?{Authorization:'Bearer '+e.NOWGO_STATUS_API_TOKEN}:{},redirect:'error',signal:AbortSignal.timeout(5000)});if(!r.ok)return reply(req,{...unknown,source:'NOWGO 상태 확인 지연'});return reply(req,resolveOfficialStatus(await r.json(),menu.placeId,menu.id))}catch{return reply(req,{...resolveOfficialStatus(null,'',''),source:'NOWGO 상태 확인 지연'})}}
