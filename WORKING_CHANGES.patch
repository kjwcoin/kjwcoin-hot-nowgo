diff --git a/components/customer-panel.tsx b/components/customer-panel.tsx
index ab718d8..a24db0e 100644
--- a/components/customer-panel.tsx
+++ b/components/customer-panel.tsx
@@ -5,17 +5,18 @@ import {Dialog,DialogContent,DialogTitle,DialogDescription} from '@/components/u
 import {Button} from '@/components/ui/button';
 import {LEVELS} from '@/lib/loyalty';
 import {api} from '@/lib/client';
+import {returnPath} from '@/lib/integration-policy';
 type State={customer:{id:string;nickname:string;phoneMasked:string;phoneVerified:boolean}|null;points:number|null;level:typeof LEVELS[number];auth:{ready:boolean;accountUrl:string|null}};
 export default function CustomerPanel(){
- const [open,setOpen]=useState(false),[data,setData]=useState<State|null>(null),[error,setError]=useState(''),[busy,setBusy]=useState(false);
+ const [open,setOpen]=useState(false),[data,setData]=useState<State|null>(null),[error,setError]=useState(''),[busy,setBusy]=useState(false),[returnTo,setReturnTo]=useState('/map');
  async function refresh(){try{setData(await api<State>('/api/customer/me'));setError('')}catch{setError('회원 정보를 불러오지 못했어요. 잠시 후 다시 열어주세요.')}}
  useEffect(()=>{void refresh();const changed=()=>void refresh();window.addEventListener('hot-customer-change',changed);return()=>window.removeEventListener('hot-customer-change',changed)},[]);
  const next=LEVELS.find(l=>l.min>(data?.points||0));
  async function logout(){setBusy(true);try{await api('/api/customer/logout',{method:'POST'});await refresh();window.dispatchEvent(new Event('hot-customer-change'))}catch(e){setError((e as Error).message)}finally{setBusy(false)}}
- return <><Button className="customer-trigger" variant="ghost" onClick={()=>{setOpen(true);void refresh()}} aria-label={data?.customer?'내 활동':'통합 로그인'}><UserRound size={18}/><span>{data?.customer?'내 활동':'통합 로그인'}</span></Button><Dialog open={open} onOpenChange={setOpen}><DialogContent className="customer-dialog"><DialogTitle className="customer-title">{data?.customer?`${data.customer.nickname}님의 맛부심`:'NOWGO 통합회원'}</DialogTitle><DialogDescription>하나의 계정으로 메뉴 발견부터 리뷰와 단골까지.</DialogDescription>
+ return <><Button className="customer-trigger" variant="ghost" onClick={()=>{setReturnTo(returnPath(window.location.pathname+window.location.search+window.location.hash));setOpen(true);void refresh()}} aria-label={data?.customer?'내 활동':'통합 로그인·회원가입'}><UserRound size={18}/><span>{data?.customer?'내 활동':'통합 로그인·회원가입'}</span></Button><Dialog open={open} onOpenChange={setOpen}><DialogContent className="customer-dialog"><DialogTitle className="customer-title">{data?.customer?`${data.customer.nickname}님의 맛부심`:'NOWGO 통합회원'}</DialogTitle><DialogDescription>한 번 가입하고 HOT와 NOWGO를 같은 계정으로 이용해요.</DialogDescription>
  {!data&&!error&&<p>회원 정보를 확인하고 있어요.</p>}
- {data&&!data.customer&&<><div className="unified-auth-message"><strong>가입은 한 번이면 돼요.</strong><p>닉네임·휴대폰 번호·비밀번호는 NOWGO 통합회원에서 관리해요. HOT에서는 별도 회원이나 비밀번호를 만들지 않습니다.</p></div>{data.auth.ready?<div className="unified-auth-actions"><a href="/api/auth/start?returnTo=%2F%23report">통합회원 가입·로그인 <ArrowUpRight size={17}/></a></div>:<p className="customer-note" role="status">통합회원 연결 준비 중입니다. 지도와 메뉴는 지금 둘러볼 수 있고 제보 제출은 연결 후 열립니다.</p>}<p className="customer-note">점주도 같은 계정을 사용해요. 매장 소유권 확인을 마쳐야 공식 영업·품절 상태를 알릴 수 있습니다.</p><a className="text-link" href="/terms">통합회원·제보 원칙 보기 ↗</a></>}
- {data?.customer&&<><div className="customer-level"><span className="customer-level-number">{data.level.level}</span><div><small>나의 기여 레벨</small><h3>{data.level.name}</h3><p>{data.points===null?'기여 기록 연결 준비 중':`확인된 기여 ${data.points}점${next?` · 다음 단계까지 ${next.min-data.points}점`:''}`}</p></div></div><p className="customer-note">리뷰는 첫 이용부터 매장 미니홈피에서 작성해요. 레벨은 확인된 기여로 올라가고 공식 점주 권한과는 별개예요.</p><details className="customer-benefits"><summary>레벨별 성장 조건</summary>{LEVELS.map(l=><div key={l.level}><b>Lv.{l.level} {l.name} · {l.min}점</b><span>{l.benefit}</span></div>)}</details><p className="customer-note">{data.customer.phoneMasked} · {data.customer.phoneVerified?'번호 인증됨':'번호 미인증'}</p>{data.auth.accountUrl&&<a className="text-link" href={data.auth.accountUrl} target="_blank" rel="noreferrer">통합회원 정보·수신 동의 관리 ↗</a>}<Button variant="ghost" disabled={busy} onClick={logout}>HOT에서 로그아웃</Button></>}
+ {data&&!data.customer&&<><div className="unified-auth-message"><strong>가입은 한 번이면 돼요.</strong><p>NOWGO에서 소셜 계정으로 가입하고 닉네임을 정해요. 기존 회원은 같은 계정으로 로그인하세요.</p></div>{data.auth.ready?<div className="unified-auth-actions"><a href={"/api/auth/start?returnTo="+encodeURIComponent(returnTo)}>통합 로그인·회원가입 <ArrowUpRight size={17}/></a></div>:<p className="customer-note" role="status">통합회원 연결 준비 중입니다. 지도와 메뉴는 지금 둘러볼 수 있고 제보 제출은 연결 후 열립니다.</p>}<p className="customer-note">점주도 같은 계정을 사용해요. 매장 소유권 확인을 마쳐야 공식 영업·품절 상태를 알릴 수 있습니다.</p><a className="text-link" href="/terms">통합회원·제보 원칙 보기 ↗</a></>}
+ {data?.customer&&<><div className="customer-level"><span className="customer-level-number">{data.level.level}</span><div><small>나의 기여 레벨</small><h3>{data.level.name}</h3><p>{data.points===null?'기여 기록 연결 준비 중':`확인된 기여 ${data.points}점${next?` · 다음 단계까지 ${next.min-data.points}점`:''}`}</p></div></div><p className="customer-note">리뷰는 연결된 매장 미니홈피에서 이용할 수 있어요. 레벨은 확인된 기여로 올라가고 공식 점주 권한과는 별개예요.</p><details className="customer-benefits"><summary>레벨별 성장 조건</summary>{LEVELS.map(l=><div key={l.level}><b>Lv.{l.level} {l.name} · {l.min}점</b><span>{l.benefit}</span></div>)}</details><p className="customer-note">{data.customer.phoneMasked} · {data.customer.phoneVerified?'번호 인증됨':'번호 미인증'}</p>{data.auth.accountUrl&&<a className="text-link" href={data.auth.accountUrl} target="_blank" rel="noreferrer">내 NOWGO 계정 보기 ↗</a>}<Button variant="ghost" disabled={busy} onClick={logout}>HOT에서 로그아웃</Button></>}
  {error&&<p className="customer-error" role="alert">{error}</p>}
  </DialogContent></Dialog></>
 }
diff --git a/components/hot-app.tsx b/components/hot-app.tsx
index e618667..07ebb84 100644
--- a/components/hot-app.tsx
+++ b/components/hot-app.tsx
@@ -13,6 +13,8 @@ const loadCatalog=()=>api<{menus:Menu[]}>('/api/menus').then(d=>setCatalog([...M
 useEffect(()=>{loadCatalog();const refresh=()=>api<{customer:unknown}>('/api/customer/me').then(d=>setCustomerReady(!!d.customer)).catch(()=>{});refresh();window.addEventListener('hot-customer-change',refresh);return()=>window.removeEventListener('hot-customer-change',refresh)},[]);
 const [active,setActive]=useState(0),[flavor,setFlavor]=useState('전체'),[heat,setHeat]=useState(5),[budget,setBudget]=useState(20000),[query,setQuery]=useState(''),[selected,setSelected]=useState<Menu|null>(null),[saved,setSaved]=useState<string[]>([]),[reports,setReports]=useState<Report[]>([]),[savedOpen,setSavedOpen]=useState(false),[toast,setToast]=useState(''),[pending,setPending]=useState(''),[ready,setReady]=useState(false),[role,setRole]=useState('customer'),[reportMenu,setReportMenu]=useState(''),[photoName,setPhotoName]=useState(''),[result,setResult]=useState(''),[point,setPoint]=useState<{address:string,lat:number,lng:number}|null>(null);
 const form=useRef<HTMLFormElement>(null),requestId=useRef('');const filtered=useMemo(()=>catalog.filter(m=>(flavor==='전체'||m.flavor===flavor)&&m.heat<=heat&&m.price<=budget&&`${m.name} ${m.area}`.includes(query)),[catalog,flavor,heat,budget,query]);
+useEffect(()=>{try{const raw=sessionStorage.getItem('nowgo-report-draft');if(!raw)return;const d=JSON.parse(raw);sessionStorage.removeItem('nowgo-report-draft');if(!d||!Number.isFinite(d.at)||Date.now()-d.at>86400000||!form.current)return;for(const [name,value]of Object.entries(d.values||{})){const el=form.current.elements.namedItem(name);if((el instanceof HTMLInputElement||el instanceof HTMLTextAreaElement||el instanceof HTMLSelectElement)&&!['file','checkbox','hidden'].includes(el.type)&&typeof value==='string')el.value=value.slice(0,2000)}setRole(d.role==='owner'?'owner':'customer');setReportMenu(typeof d.menuId==='string'?d.menuId:'');if(d.point&&Number.isFinite(d.point.lat)&&Number.isFinite(d.point.lng)&&typeof d.point.address==='string')setPoint(d.point);setResult('작성하던 내용을 불러왔어요. 사진은 다시 첨부하고 공개 동의를 확인해 주세요.')}catch{}},[]);
+useEffect(()=>{const preserve=()=>{try{if(!form.current)return;const values:Record<string,string>={};for(const el of Array.from(form.current.elements)){if((el instanceof HTMLInputElement||el instanceof HTMLTextAreaElement||el instanceof HTMLSelectElement)&&el.name&&!['file','checkbox','hidden'].includes(el.type))values[el.name]=el.value}if(!values.shop&&!values.menu)return;sessionStorage.setItem('nowgo-report-draft',JSON.stringify({at:Date.now(),values,role,menuId:reportMenu,point}))}catch{}};window.addEventListener('pagehide',preserve);return()=>window.removeEventListener('pagehide',preserve)},[role,reportMenu,point]);
 useEffect(()=>{requestId.current=uuid();api<{saved:string[],reports:Report[]}>('/api/state').then(d=>{setSaved(d.saved);setReports(d.reports);setReady(true)}).catch(()=>setToast('보관함 연결을 확인하고 있어요. 메뉴 탐색은 계속할 수 있습니다.'));const id=new URLSearchParams(location.search).get('menu');if(id)setSelected(MENUS.find(x=>x.id===id)||null)},[]);
 useEffect(()=>{const reload=()=>api<{saved:string[],reports:Report[]}>('/api/state').then(d=>{setSaved(d.saved);setReports(d.reports)}).catch(()=>{});window.addEventListener('hot-customer-change',reload);return()=>window.removeEventListener('hot-customer-change',reload)},[]);
 useEffect(()=>{if(!toast)return;const t=setTimeout(()=>setToast(''),5000);return()=>clearTimeout(t)},[toast]);
@@ -22,7 +24,7 @@ function open(m:Menu){setSelected(m);track('menu_detail_open',m.id)}
 async function save(m:Menu){if(pending)return;setPending(m.id);try{const value=!saved.includes(m.id);await api('/api/state',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({menuId:m.id,saved:value})});setSaved(a=>value?[...a,m.id]:a.filter(x=>x!==m.id));setToast(value?'보관함에 담았어요. 다음에 당길 때 다시 만나요.':'보관함에서 꺼냈어요.')}catch(e){setToast((e as Error).message)}finally{setPending('')}}
 async function share(m:Menu){try{await navigator.clipboard.writeText(`${location.origin}/?menu=${m.id}`);setToast('이 메뉴의 링크를 복사했어요.')}catch{setToast('주소창에서 링크를 복사해 주세요.')}}
 function report(m?:Menu,owner=false){setSelected(null);setRole(owner?'owner':'customer');setReportMenu(m?.id||'');setResult('');setTimeout(()=>{if(m&&form.current){const f=form.current;for(const [k,v]of Object.entries({shop:m.shop,menu:m.name,price:m.price,heat:m.heat,flavor:m.flavor})){const el=f.elements.namedItem(k) as HTMLInputElement|null;if(el)el.value=String(v)}}document.getElementById('report')?.scrollIntoView({behavior:'smooth'})},50)}
-async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();if(pending)return;if(!customerReady){setResult('상단 통합 로그인에서 가입 또는 로그인한 뒤 제출해 주세요. 입력 내용은 유지됩니다.');return}setPending('report');setResult('');const f=new FormData(e.currentTarget);f.set('requestId',requestId.current);f.set('role',role);f.set('menuId',reportMenu);if(point){f.set('lat',String(point.lat));f.set('lng',String(point.lng))}try{const d=await api<{status:string}>('/api/reports',{method:'POST',body:f});setResult(d.status==='published_unverified'?'지도에 고객 제보로 등록했어요. 공식 점주·영업 상태는 아직 미확인입니다.':d.status==='held_for_review'?'확인이 필요한 제보로 접수했어요. 확인 전에는 지도에 공개되지 않습니다.':'가매장 참고 제보를 저장했어요. 실제 매장 정보로 공개되지 않습니다.');requestId.current=uuid();await loadCatalog();const state=await api<{reports:Report[]}>('/api/state');setReports(state.reports);form.current?.reset();setPhotoName('');setPoint(null);setReportMenu('')}catch(e){setResult((e as Error).message)}finally{setPending('')}}
+async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();if(pending)return;if(!customerReady){setResult('상단 통합 로그인·회원가입 후 제출해 주세요. 이 탭에서는 작성한 글을 임시 보관합니다. 사진은 다시 첨부해 주세요.');return}setPending('report');setResult('');const f=new FormData(e.currentTarget);f.set('requestId',requestId.current);f.set('role',role);f.set('menuId',reportMenu);if(point){f.set('lat',String(point.lat));f.set('lng',String(point.lng))}try{const d=await api<{status:string}>('/api/reports',{method:'POST',body:f});setResult(d.status==='published_unverified'?'지도에 고객 제보로 등록했어요. 공식 점주·영업 상태는 아직 미확인입니다.':d.status==='held_for_review'?'확인이 필요한 제보로 접수했어요. 확인 전에는 지도에 공개되지 않습니다.':'가매장 참고 제보를 저장했어요. 실제 매장 정보로 공개되지 않습니다.');requestId.current=uuid();await loadCatalog();const state=await api<{reports:Report[]}>('/api/state');setReports(state.reports);form.current?.reset();setPhotoName('');setPoint(null);setReportMenu('')}catch(e){setResult((e as Error).message)}finally{setPending('')}}
 async function remove(id:string){try{await api('/api/reports',{method:'DELETE',headers:{'Content-Type':'application/json'},body:JSON.stringify({id})});setReports(a=>a.filter(r=>r.id!==id));await loadCatalog();setToast('제보와 첨부 사진을 삭제했어요.')}catch(e){setToast((e as Error).message)}}
 useEffect(()=>{const ctx=(document as unknown as {modelContext?:{registerTool:(t:unknown,o:unknown)=>Promise<void>}}).modelContext;if(!ctx)return;const ctrl=new AbortController();void Promise.resolve(ctx.registerTool({name:'filter_hot_menus',title:'매운 메뉴 고르기',description:'현재 화면의 맛 필터와 맵기 상한을 변경합니다. 저장·제보를 하지 않습니다.',inputSchema:{type:'object',properties:{flavor:{type:'string',enum:FLAVORS},maxHeat:{type:'integer',minimum:1,maximum:5}},required:['flavor','maxHeat'],additionalProperties:false},annotations:{readOnlyHint:true},execute:(input:unknown)=>{const p=input as {flavor:string,maxHeat:number};if(!FLAVORS.includes(p.flavor)||!Number.isInteger(p.maxHeat)||p.maxHeat<1||p.maxHeat>5)throw new Error('Invalid filters');setFlavor(p.flavor);setHeat(p.maxHeat);document.getElementById('discover')?.scrollIntoView();return {menus:MENUS.filter(m=>(p.flavor==='전체'||m.flavor===p.flavor)&&m.heat<=p.maxHeat).map(m=>m.name),isExample:true}}},{signal:ctrl.signal})).catch(()=>{});return()=>ctrl.abort()},[]);
 return <><header className="site-header"><Brand/><nav><a href="/map">메뉴 지도</a><a href="#report">한 접시 제보</a><a href="#owner" onClick={()=>track('owner_info_view','owner-section')}>사장님이라면</a></nav><button className="saved-nav" onClick={()=>setSavedOpen(true)} aria-label={`내 보관함 ${saved.length}개`}><Bookmark size={18}/><span>보관함</span><b>{saved.length}</b></button><CustomerPanel/></header><main>
@@ -30,7 +32,7 @@ return <><header className="site-header"><Brand/><nav><a href="/map">메뉴 지
 <section className="section" id="discover"><div className="section-top"><div><div className="eyebrow">02 / FIND YOUR PLATE</div><h2>지금 당기는<br/>한 접시를 골라요.</h2></div><p>이름만 보고 고르기 어려운 날.<br/>맛, 맵기, 한 끼 예산으로 좁혀보세요.</p></div><div className="filter-bar"><div className="flavor-filters">{FLAVORS.map(f=><button key={f} className={flavor===f?'active':''} aria-pressed={flavor===f} onClick={()=>setFlavor(f)}>{f}</button>)}</div><label className="budget-control">한 끼 예산 <select value={budget} onChange={e=>setBudget(Number(e.target.value))}><option value={20000}>2만원 이하</option><option value={10000}>1만원 이하</option><option value={7000}>7천원 이하</option></select></label></div><div className="map-search"><div><MapPin size={16}/><b>서울 · 연남동</b><span>첫 번째 동네</span></div><input aria-label="메뉴 검색" placeholder="당기는 메뉴 검색" value={query} onChange={e=>setQuery(e.target.value)}/></div><KakaoMap menus={filtered} onSelect={open} onPoint={p=>{setPoint(p);document.getElementById('report')?.scrollIntoView()}}/><div className="result-top"><span>이렇게 먹고 싶은 날 <b>{filtered.length}</b></span><span>가매장 메뉴로 흐름을 체험해 보세요</span></div><div className="menu-grid">{filtered.map(m=><article key={m.id} className="menu-card" data-menu={m.id}><button className="menu-photo" onClick={()=>open(m)}><img src={m.image} alt={`${m.name} 참고 사진`} loading="lazy"/><span className="menu-heat">{HEAT[m.heat-1]}</span><span className="photo-ref">참고 사진</span></button><div className="menu-meta"><span>{m.area}</span><button className={saved.includes(m.id)?'bookmark active':'bookmark'} aria-label={`${m.name} ${saved.includes(m.id)?'저장 취소':'저장'}`} onClick={()=>save(m)} disabled={pending===m.id}><Bookmark size={20} fill={saved.includes(m.id)?'currentColor':'none'}/></button></div><button className="menu-title" onClick={()=>open(m)}><span>{m.isDemo?'[가매장] ':''}{m.name}</span><ArrowUpRight size={21}/></button><div className="menu-bottom"><strong>{money(m.price)} <small>{m.isDemo?'예시':'제보'}</small></strong><span>{m.flavor}</span></div><div className="status-line"><Clock size={12}/>영업·품절 확인 필요</div></article>)}{!filtered.length&&<div className="empty-state"><h3>아직 이 취향의 한 접시가 없어요.</h3><p>조건을 넓히거나 알고 있는 메뉴를 알려주세요.</p><Button variant="outline" onClick={()=>{setFlavor('전체');setHeat(5);setBudget(20000);setQuery('')}}>전체 메뉴 다시 보기</Button></div>}</div></section>
 <section className="section taste-section"><div><div className="eyebrow">03 / YOUR KIND OF HOT</div><h2>잘 먹는 맵기보다,<br/>좋아하는 맵기.</h2><p>맛의 결은 취향, 맵기는 강도.<br/>오늘 편안하게 먹고 싶은 정도를 골라요.</p></div><div className="heat-panel"><div className="heat-heading"><span>오늘의 맵기 상한</span><strong>{HEAT[heat-1]}</strong></div><div className="heat-scale">{HEAT.map((h,i)=><button key={h} className={heat===i+1?'selected':''} aria-pressed={heat===i+1} onClick={()=>setHeat(i+1)}><span style={{background:['#e6c993','#da9c63','#cf7147','#c34227','#922c1c'][i]}}>0{i+1}</span><small>{h}</small></button>)}</div><p className="small-note">사람마다 느끼는 맵기는 달라요. 점주 안내와 손님 체감을 함께 확인하세요.</p><a className="text-link" href="#discover">이 맵기까지 메뉴 보기 <ArrowRight size={18}/></a></div></section>
 <section className="section before-go"><div className="status-preview"><div className="mini-brand"><img className="inline-nowgo-logo" src="/images/nowgo-red.png" alt="NOWGO"/> <span>매장 확인 화면 예시</span></div><h3>먹고 싶은 건 골랐죠.<br/>이제, 갈 수 있는지.</h3>{['오늘 영업','메뉴 품절','최근 확인'].map((s,i)=><div className="status-preview-row" key={s}><span>{s}</span><strong>{i===2?'아직 제보가 없어요':'확인 필요'}</strong></div>)}<a className="status-link" href="/go/demo-place-01" onClick={()=>track('place_status_open','demo-place-01')}>매장 확인 흐름 보기 <ArrowUpRight size={20}/></a><p>확인되지 않은 상태를 ‘영업 중’으로 표시하지 않아요.</p></div><div><div className="eyebrow">04 / BEFORE YOU GO</div><h2>맛있게 가고,<br/>헛걸음은 줄이고.</h2><p>문 닫힌 가게 앞에서 아쉬웠던 날.<br/>품절인 줄 모르고 찾아갔던 날.<br/>NOWGO에서 가게의 지금을 확인하고 출발해요.</p><div className="inline-promise"><Check size={18}/>확인한 사람 · 확인한 시각 · 달라진 정보</div><p className="small-note">매장 정보 연결은 시안 단계입니다.<br/>실제 운영 정보는 점주의 확인과 NOWGO 연결 후 제공해요.</p></div></section>
-<section className="section report-section" id="report"><div className="report-copy"><div className="eyebrow">05 / ONE PLATE, ONE TIP</div><h2>이 메뉴는<br/>내가 좀 알지.</h2><p>메뉴 이름과 가격. 직접 찍은 사진 한 장.<br/>먹어본 맵기와 확인한 날짜까지.<br/>당신의 맛부심을 정확한 제보로 남겨요.</p><div className="report-types">손님의 오늘 한 끼 <Plus size={17}/> 사장님의 정확한 정보</div><div className="report-note"><Camera size={23}/><p>직접 찍은 메뉴나 메뉴판을 올려주세요.<br/>사진만으로 방문 인증이나<br/>공식 점주 표시가 생기지는 않아요.</p></div></div><form ref={form} className="report-form" onSubmit={submit}><p className="report-login-status">{customerReady?'로그인됨 · 조건을 통과한 새 메뉴는 지도에 자동 등록됩니다.':'입력은 자유롭게 · 제출하려면 상단 통합 로그인을 이용해 주세요.'}</p><div className="role-picker"><button type="button" className={role==='customer'?'active':''} onClick={()=>setRole('customer')}>손님으로 제보</button><button type="button" className={role==='owner'?'active':''} onClick={()=>setRole('owner')}>점주·요리사로 제보</button></div><div className="form-grid"><label>가게 이름<Input name="shop" required minLength={2} maxLength={100} placeholder="간판에 적힌 이름"/></label><label>실제 메뉴 이름<Input name="menu" required minLength={2} maxLength={100} placeholder="메뉴판 그대로"/></label><label className="wide">서울 주소<Input name="address" placeholder="서울 마포구 …" required value={point?.address} onChange={point?e=>setPoint({...point,address:e.target.value}):undefined}/><small>지도 연결 후 위치를 눌러 앵커를 지정할 수 있어요.</small></label><label>가격 · 원<Input name="price" type="number" required min={100} max={1000000} step={1} placeholder="9500"/></label><label>직접 확인한 날짜<Input name="observedAt" type="date" required max={new Date().toISOString().slice(0,10)}/></label><label>메뉴 종류<select name="category">{MENU_CATEGORIES.map(c=><option key={c}>{c}</option>)}</select></label><label>메뉴의 맵기<select name="heat" defaultValue="3">{HEAT.map((h,i)=><option key={h} value={i+1}>{h}</option>)}</select></label><label>맛의 결<select name="flavor">{FLAVORS.slice(1).map(f=><option key={f}>{f}</option>)}</select></label><label className="wide upload-field"><Camera size={22}/><strong>{photoName||'직접 찍은 사진 한 장'}</strong><span>JPG · PNG · WebP / 2MB 이하</span><input type="file" name="photo" aria-label="메뉴 사진 첨부" accept="image/jpeg,image/png,image/webp" required onChange={e=>setPhotoName(e.target.files?.[0]?.name||'')}/></label><label className="wide">함께 알려주고 싶은 사실<Textarea name="note" maxLength={1000} placeholder="예: 가격이 바뀌었어요 / 오후에만 팔아요"/></label></div><label className="consent"><input type="checkbox" name="rights" value="yes" required/><span>직접 촬영했거나 사용 권한이 있는 사진이며, 지도와 메뉴 정보에 공개하는 데 동의해요. 얼굴·연락처는 가려주세요.</span></label><label className="consent"><input type="checkbox" name="accuracy" value="yes" required/><span>직접 확인한 사실을 정확히 작성했습니다. 허위·권리침해 제보는 정정·삭제 및 이용 제한 대상이 될 수 있음에 동의합니다. <a href="/terms" target="_blank" rel="noreferrer">제보 원칙 보기</a></span></label><p className="small-note">정상 조건을 통과한 새 메뉴와 사진은 지도에 고객 제보로 공개됩니다. 가매장 제보는 비공개입니다. 제출은 점주 확인이나 영업·품절 상태 확정이 아니며, 내 보관함에서 삭제할 수 있습니다.</p><Button type="submit" className="submit-button" disabled={pending==='report'}>{pending==='report'?<><LoaderCircle className="spin" size={18}/>저장하는 중</>:<>한 접시 알려주기 <ArrowUpRight size={20}/></>}</Button>{result&&<p className="form-result" role="status">{result}</p>}</form></section>
+<section className="section report-section" id="report"><div className="report-copy"><div className="eyebrow">05 / ONE PLATE, ONE TIP</div><h2>이 메뉴는<br/>내가 좀 알지.</h2><p>메뉴 이름과 가격. 직접 찍은 사진 한 장.<br/>먹어본 맵기와 확인한 날짜까지.<br/>당신의 맛부심을 정확한 제보로 남겨요.</p><div className="report-types">손님의 오늘 한 끼 <Plus size={17}/> 사장님의 정확한 정보</div><div className="report-note"><Camera size={23}/><p>직접 찍은 메뉴나 메뉴판을 올려주세요.<br/>사진만으로 방문 인증이나<br/>공식 점주 표시가 생기지는 않아요.</p></div></div><form ref={form} className="report-form" onSubmit={submit}><p className="report-login-status">{customerReady?'로그인됨 · 조건을 통과한 새 메뉴는 지도에 자동 등록됩니다.':'입력은 자유롭게 · 제출하려면 상단 통합 로그인·회원가입을 이용해 주세요.'}</p><div className="role-picker"><button type="button" className={role==='customer'?'active':''} onClick={()=>setRole('customer')}>손님으로 제보</button><button type="button" className={role==='owner'?'active':''} onClick={()=>setRole('owner')}>점주·요리사로 제보</button></div><div className="form-grid"><label>가게 이름<Input name="shop" required minLength={2} maxLength={100} placeholder="간판에 적힌 이름"/></label><label>실제 메뉴 이름<Input name="menu" required minLength={2} maxLength={100} placeholder="메뉴판 그대로"/></label><label className="wide">서울 주소<Input name="address" placeholder="서울 마포구 …" required value={point?.address} onChange={point?e=>setPoint({...point,address:e.target.value}):undefined}/><small>지도 연결 후 위치를 눌러 앵커를 지정할 수 있어요.</small></label><label>가격 · 원<Input name="price" type="number" required min={100} max={1000000} step={1} placeholder="9500"/></label><label>직접 확인한 날짜<Input name="observedAt" type="date" required max={new Date().toISOString().slice(0,10)}/></label><label>메뉴 종류<select name="category">{MENU_CATEGORIES.map(c=><option key={c}>{c}</option>)}</select></label><label>메뉴의 맵기<select name="heat" defaultValue="3">{HEAT.map((h,i)=><option key={h} value={i+1}>{h}</option>)}</select></label><label>맛의 결<select name="flavor">{FLAVORS.slice(1).map(f=><option key={f}>{f}</option>)}</select></label><label className="wide upload-field"><Camera size={22}/><strong>{photoName||'직접 찍은 사진 한 장'}</strong><span>JPG · PNG · WebP / 2MB 이하</span><input type="file" name="photo" aria-label="메뉴 사진 첨부" accept="image/jpeg,image/png,image/webp" required onChange={e=>setPhotoName(e.target.files?.[0]?.name||'')}/></label><label className="wide">함께 알려주고 싶은 사실<Textarea name="note" maxLength={1000} placeholder="예: 가격이 바뀌었어요 / 오후에만 팔아요"/></label></div><label className="consent"><input type="checkbox" name="rights" value="yes" required/><span>직접 촬영했거나 사용 권한이 있는 사진이며, 지도와 메뉴 정보에 공개하는 데 동의해요. 얼굴·연락처는 가려주세요.</span></label><label className="consent"><input type="checkbox" name="accuracy" value="yes" required/><span>직접 확인한 사실을 정확히 작성했습니다. 허위·권리침해 제보는 정정·삭제 및 이용 제한 대상이 될 수 있음에 동의합니다. <a href="/terms" target="_blank" rel="noreferrer">제보 원칙 보기</a></span></label><p className="small-note">정상 조건을 통과한 새 메뉴와 사진은 지도에 고객 제보로 공개됩니다. 가매장 제보는 비공개입니다. 제출은 점주 확인이나 영업·품절 상태 확정이 아니며, 내 보관함에서 삭제할 수 있습니다.</p><Button type="submit" className="submit-button" disabled={pending==='report'}>{pending==='report'?<><LoaderCircle className="spin" size={18}/>저장하는 중</>:<>한 접시 알려주기 <ArrowUpRight size={20}/></>}</Button>{result&&<p className="form-result" role="status">{result}</p>}</form></section>
 <section className="section owner-section" id="owner"><div className="section-top"><div><div className="eyebrow">06 / FOR THE PEOPLE WHO COOK</div><h2>한 번 온 손님이,<br/>다시 오는 손님으로.</h2></div><p>사장님이 정확하게 알려주는 오늘의 정보.<br/>손님이 믿고 다시 찾을 이유가 됩니다.</p></div><div className="owner-rows">{[['우리 메뉴, 내가 바로잡기','이름과 가격이 다르다면 먼저 정정을 남겨요. 관리 권한 확인 후 공식 정보로 연결합니다.'],['품절도 소식이 되도록','영업·품절·재개 정보를 같은 매장 링크로. 헛걸음 대신 다음 방문을 준비하는 운영 목표예요.'],['단골과 이어지는 작은 접점','저장한 손님이 다시 찾을 수 있는 매장 페이지. 새 소식과 혜택은 동의한 손님에게만 전해요.']].map(([a,b],i)=><div key={a}><span>0{i+1}</span><h3>{a}</h3><p>{b}</p></div>)}</div><div className="owner-actions"><button className="text-link" onClick={()=>{track('owner_info_view','report');report(undefined,true)}}>우리 가게 정보 알려주기 <ArrowUpRight size={20}/></button><a className="quiet-link" href="https://www.nowgo.space/owner/login" target="_blank" rel="noreferrer">나우고 점주 가입하기 <ExternalLink size={16}/></a></div><p className="small-note">단골 소식·공식 점주 관리·성과 요약은 NOWGO 연동 후 제공할 기능입니다. 매출이나 방문 수를 보장하지 않아요.</p></section>
 <section className="section regular-section"><div><div className="eyebrow">07 / OUR FIRST FIVE</div><h2>맛잘알의 기준은<br/>다음 사람이 믿는 정보.</h2><p>좋아하는 메뉴를 발견하고 달라진 사실을 알려요.<br/>확인된 기여가 쌓이면 나의 맛부심도 자라요.</p><div className="regular-benefit"><Bookmark size={18}/>메뉴 저장 → 상태 확인 → 방문 → 미니홈피 리뷰</div><div className="regular-benefit"><Heart size={18}/>레벨 1부터 리뷰 가능 · 확인된 기여로 성장</div><button className="text-link" onClick={()=>setSavedOpen(true)}>나의 한 끼 보관함 <ArrowUpRight size={20}/></button></div><div className="founders">{['점심시간이 짧은 직장인','혼자서 한 그릇 찾는 사람','국물부터 생각나는 사람','친구들의 메뉴 결정 담당','한 끼 예산을 지키는 사람'].map((r,i)=><div key={r}><span className="founder-number">0{i+1}</span><strong>{r}</strong><span className="founder-label">함께할 맛잘알</span></div>)}<p>실제 참여자를 모집하기 위한 5가지 상황입니다.<br/>가입자나 후기 수를 꾸며내지 않아요.</p></div></section>
 <section className="section closing"><div className="eyebrow">08 / GOOD FOOD, LESS DETOURS</div><h2>가게엔 단골을.<br/>손님에겐 여유를.</h2><div className="closing-bottom"><p>내 시간과 한 끼의 예산이 아깝지 않게.<br/>매운맛에서 시작해, 더 편한 동네 생활로.</p><a className="closing-link" href="/map">내 취향 메뉴 찾기 <ArrowUpRight size={40}/></a></div><div className="closing-word" aria-hidden="true">HOT, NOW.</div></section></main>
diff --git a/lib/integration-policy.ts b/lib/integration-policy.ts
index adce55d..5b5f737 100644
--- a/lib/integration-policy.ts
+++ b/lib/integration-policy.ts
@@ -3,7 +3,15 @@ export function nowgoUrl(raw:unknown):URL|null {
  if(typeof raw!=='string')return null;
  try{const u=new URL(raw);return u.protocol==='https:'&&['nowgo.space','www.nowgo.space'].includes(u.hostname)&&!u.username&&!u.password&&!u.port?u:null}catch{return null}
 }
-export function returnPath(raw:unknown){return typeof raw==='string'&&['/map','/','#report','/#report'].includes(raw)?(raw==='#report'?'/#report':raw):'/map'}
+export function returnPath(raw:unknown){
+ if(raw==='#report')return '/#report';
+ if(typeof raw!=='string'||raw.length>1500||!raw.startsWith('/')||raw.startsWith('//')||/[\\\u0000-\u0020]/.test(raw))return '/map';
+ try{const u=new URL(raw,'https://hot.local');if(u.origin!=='https://hot.local'||!(/^(?:\/|\/map|\/place\/[-a-zA-Z0-9_]{1,100})$/.test(u.pathname)))return '/map';
+ // Keep only display/filter state; never carry another redirect or auth material.
+ for(const k of [...u.searchParams.keys()])if(!['q','taste','spice','budget','menu'].includes(k))u.searchParams.delete(k);
+ if(u.hash&&!/^#[a-zA-Z0-9_-]{1,80}$/.test(u.hash))u.hash='';
+ return u.pathname+u.search+u.hash;}catch{return '/map'}
+}
 export type OfficialStatus={linked:boolean;open:string;menu:string;checkedAt:string|null;source:string;fresh:boolean;url:string|null;validUntil:string|null};
 export function resolveOfficialStatus(raw:unknown,placeId:string,menuId:string,now=Date.now()):OfficialStatus {
  const unknown:OfficialStatus={linked:false,open:'확인 필요',menu:'확인 필요',checkedAt:null,source:'NOWGO 매장 연결 준비 중',fresh:false,url:null,validUntil:null};
diff --git a/tests/integration-policy.test.mjs b/tests/integration-policy.test.mjs
index f0ed9e0..876ed79 100644
--- a/tests/integration-policy.test.mjs
+++ b/tests/integration-policy.test.mjs
@@ -5,7 +5,7 @@ const now=Date.parse('2026-09-24T06:00:00Z');
 const current={hot_place_id:'reported-1',place_id:'real-place',minihome_url:'https://nowgo.space/p/real-place',owner_verified:true,source:'owner',status:'OPEN',observed_at:'2026-09-24T05:30:00Z',expires_at:'2026-09-24T07:00:00Z',menu:{hot_menu_id:'menu-1',status:'SOLD_OUT',observed_at:'2026-09-24T05:45:00Z',expires_at:'2026-09-24T07:00:00Z'}};
 const resolve=d=>resolveOfficialStatus(d,'reported-1','menu-1',now);
 test('trust only exact HTTPS NOWGO origins without credentials',()=>{for(const value of ['http://nowgo.space/p/a','https://nowgo.space.evil.test/p/a','https://user@nowgo.space/p/a','https://nowgo.space:444/p/a','javascript:alert(1)'])assert.equal(nowgoUrl(value),null);assert.ok(nowgoUrl(current.minihome_url))});
-test('return destination is restricted to known local pages',()=>{for(const v of ['//evil.test','https://evil.test','/\\evil.test','/api/auth/callback','/map?next=https://evil.test'])assert.equal(returnPath(v),'/map');assert.equal(returnPath('/#report'),'/#report')});
+test('return destination is restricted to known local pages',()=>{for(const v of ['//evil.test','https://evil.test','/\\evil.test','/api/auth/callback','/map?next=https://evil.test'])assert.equal(returnPath(v),'/map');assert.equal(returnPath('/#report'),'/#report');assert.equal(returnPath('/map?q=떡볶이&budget=10000#menu'),'/map?q=%EB%96%A1%EB%B3%B6%EC%9D%B4&budget=10000#menu');assert.equal(returnPath('/place/demo-place-01?menu=demo-menu-01'),'/place/demo-place-01?menu=demo-menu-01')});
 test('restaurant OPEN can coexist with selected dish SOLD_OUT',()=>{const s=resolve(current);assert.equal(s.open,'영업 중');assert.equal(s.menu,'품절');assert.equal(s.fresh,true)});
 test('missing menu evidence is never inferred from OPEN',()=>assert.equal(resolve({...current,menu:undefined}).menu,'확인 필요'));
 test('other menu evidence does not apply',()=>assert.equal(resolve({...current,menu:{...current.menu,hot_menu_id:'other'}}).menu,'확인 필요'));
